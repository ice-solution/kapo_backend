{
  "name": "kapo_backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "create-test-data": "node scripts/createTestClientProfile.js",
    "docker:build": "docker build -t kapo-backend .",
    "docker:run": "docker run -p 4488:4488 --env-file .env kapo-backend",
    "docker:compose": "docker-compose up -d",
    "docker:compose:build": "docker-compose up -d --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "connect-mongo": "^5.1.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "mongoose": "^8.14.0",
    "multer": "^1.4.5-lts.2",
    "puppeteer": "^24.22.0"
  }
}
